const App = () => {
  return (
    <main className="app-shell">
      <section className="card">
        <h1>NutriMetrics Web</h1>
        <p>
          React frontend initialized with Vite and pnpm. Use Docker Compose to run
          alongside the API and supporting services.
        </p>
      </section>
    </main>
  );
};

export default App;
